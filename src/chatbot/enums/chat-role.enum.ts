/**
 * Who said it. Tool results are not messages — they are the model's working,
 * and what came of them is in `ChatMessage.sources`.
 */
export enum ChatRole {
  User = 'user',
  Assistant = 'assistant',
}
