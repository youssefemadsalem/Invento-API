export const FAQ_QUESTION_MIN_LENGTH = 5;
export const FAQ_QUESTION_MAX_LENGTH = 300;
/** One character: an answer may be a bare "Yes", never an empty string. */
export const FAQ_ANSWER_MIN_LENGTH = 1;
export const FAQ_ANSWER_MAX_LENGTH = 2000;

/** Keeps "a handful of entries" true, which is why the list is not paginated. */
export const MAX_FAQS_PER_STORE = 100;
